Build the flightclaw scie binary locally using `bash build.sh`.

Before building, check if `pex` is installed. If not, install it with `pip install pex`.

After building, report the output file path and size. Verify the binary is a valid executable with `file dist/flightclaw`.
